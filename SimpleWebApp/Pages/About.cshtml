@page
@model SimpleWebApp.Pages.AboutModel
@{
    ViewData["Title"] = "About";
}

<h1>About Bala</h1>
<p>SimpleWebApp is a minimal example built with ASP.NET Core Razor Pages (.NET 8).</p>
<p>It also exposes a tiny JSON API at <code>/api/greet/{name}</code>.</p>
